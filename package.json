{
  "name": "github-auth",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://mgdskr.github.io/github-auth/",
  "dependencies": {
    "react": "^16.2.0",
    "react-dom": "^16.2.0",
    "react-scripts": "1.0.17"
  },
  "lint-staged": {
    "src/**/*.{js, jsx, json, css}": [
      "prettier --write -single-quote", "git add"
    ]

  },
  "scripts": {
    "precomit": "lint-staged",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",

  }
}
