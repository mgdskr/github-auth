import auth from './auth';
import repos from './repos';
import filters from './filters';
import sort from './sort';
import dialog from './dialog';

export default { auth, repos, filters, sort, dialog };
